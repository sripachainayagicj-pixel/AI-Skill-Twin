import { useState } from 'react'
import { ArrowRight, BarChart3, CheckCircle2, Target } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
import PageWrapper from '../components/layout/PageWrapper'
import Card from '../components/ui/Card'
import Button from '../components/ui/Button'
import LoadingSpinner from '../components/ui/LoadingSpinner'
import { useCareerGoal, useSkillGap } from '../context/CareerDataContexts'
import { useNotif } from '../context/NotifContext'
import { ROUTES } from '../utils/constants'
export default function SkillGap() { const { goal } = useCareerGoal(); const { gaps, isAnalyzed, analyzeGap, strongSkills, weakSkills, missingSkills } = useSkillGap(); const { addNotif } = useNotif(); const navigate = useNavigate(); const [loading, setLoading] = useState(false); const run = async () => { setLoading(true); await analyzeGap(); setLoading(false); addNotif({ type: 'info', title: 'Skill gap analysis done', message: 'Your role requirements were compared with your skills.' }) }; return <PageWrapper title="Skill Gap" subtitle="Prioritize what will move the needle"><div className="mx-auto max-w-5xl space-y-6">{!goal ? <Card><div className="flex flex-col items-center py-12 text-center"><Target className="text-accent-blue" size={32} /><h2 className="mt-4 text-xl font-bold text-text-primary">Set your career goal and add skills to analyze your skill gaps.</h2><div className="mt-6 flex flex-wrap justify-center gap-3"><Button onClick={() => navigate(ROUTES.CAREER_GOALS)}>Set Career Goal <ArrowRight size={15} /></Button><Button variant="secondary" onClick={() => navigate(ROUTES.SKILLS)}>Add Skills <ArrowRight size={15} /></Button></div></div></Card> : <><Card><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-wide text-accent-blue">Gap analysis</p><h2 className="mt-2 text-xl font-bold text-text-primary">{goal.targetRole}</h2><p className="mt-1 text-sm text-text-secondary">Compared against common requirements for your target role.</p></div><Button onClick={() => void run()} disabled={loading} loading={loading}><BarChart3 size={16} />Analyze Skill Gap</Button></div></Card>{loading && <Card><div className="flex items-center gap-3 text-sm text-text-secondary"><LoadingSpinner size={18} />Comparing your profile...</div></Card>}{isAnalyzed && !loading && <><div className="grid gap-4 md:grid-cols-3"><Card><p className="text-xs uppercase text-text-muted">Strong Skills</p><p className="mt-2 text-3xl font-bold text-success">{strongSkills.length}</p></Card><Card><p className="text-xs uppercase text-text-muted">Skills to Improve</p><p className="mt-2 text-3xl font-bold text-warning">{weakSkills.length}</p></Card><Card><p className="text-xs uppercase text-text-muted">Missing Skills</p><p className="mt-2 text-3xl font-bold text-danger">{missingSkills.length}</p></Card></div><Card><h3 className="font-semibold text-text-primary">Role requirements</h3><div className="mt-4 grid gap-3 sm:grid-cols-2">{gaps.map(item => <div key={item.skill} className="flex items-center gap-3 rounded-lg border border-border p-3"><CheckCircle2 size={16} className={item.status === 'Strong' ? 'text-success' : item.status === 'Improve' ? 'text-warning' : 'text-danger'} /><span className="flex-1 text-sm text-text-primary">{item.skill}</span><span className={item.priority === 'High' ? 'badge-danger' : item.priority === 'Medium' ? 'badge-warning' : 'badge-success'}>{item.status}</span></div>)}</div></Card></>}</>}</div></PageWrapper> }
