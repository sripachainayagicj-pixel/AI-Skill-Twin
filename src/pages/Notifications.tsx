import { Bell, Check, Trash2 } from 'lucide-react'
import PageWrapper from '../components/layout/PageWrapper'
import Card from '../components/ui/Card'
import Button from '../components/ui/Button'
import EmptyState from '../components/ui/EmptyState'
import { useNotif } from '../context/NotifContext'
export default function Notifications() { const { notifications, unreadCount, markRead, markAllRead, deleteNotif } = useNotif(); return <PageWrapper title="Notifications" subtitle="Stay close to the signals that matter"><div className="mx-auto max-w-3xl space-y-5"><div className="flex items-center justify-between"><div><h2 className="text-xl font-bold text-text-primary">Inbox</h2><p className="mt-1 text-sm text-text-secondary">{unreadCount} unread notification{unreadCount === 1 ? '' : 's'}.</p></div><Button variant="secondary" size="sm" disabled={!unreadCount} onClick={markAllRead}><Check size={15} />Mark All Read</Button></div>{notifications.length === 0 ? <Card><EmptyState icon={<Bell size={24} />} title="You're all caught up. ✓" description="New career signals will appear here." /></Card> : <div className="space-y-3">{notifications.map(notification => <Card key={notification.id}><div className="flex items-start gap-3"><div className={`mt-1 h-2.5 w-2.5 shrink-0 rounded-full ${notification.read ? 'bg-border' : 'bg-accent-blue'}`} /><div className="min-w-0 flex-1"><p className="text-sm font-semibold text-text-primary">{notification.title}</p><p className="mt-1 text-sm text-text-secondary">{notification.message}</p><p className="mt-2 text-xs text-text-muted">{new Date(notification.createdAt).toLocaleString()}</p></div>{!notification.read && <button className="p-2 text-xs text-accent-blue hover:text-text-primary" onClick={() => markRead(notification.id)} aria-label="Mark notification read">Mark read</button>}<button className="p-2 text-text-muted hover:text-danger" onClick={() => deleteNotif(notification.id)} aria-label="Delete notification"><Trash2 size={15} /></button></div></Card>)}</div>}</div></PageWrapper> }
